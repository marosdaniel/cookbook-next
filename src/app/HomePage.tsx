'use client';

import { Title } from '@mantine/core';

const HomePage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <Title order={1}>home</Title>
    </div>
  );
};

export default HomePage;
